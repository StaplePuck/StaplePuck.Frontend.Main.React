{
  /* <div className="page">
  {!isAuthenticated() && (
    <div>
      <img src={logo} alt="StaplePuck Logo" width="200px" height="200px" />
      <br />
      <Button
        id="qsLoginBtn"
        bsStyle="primary"
        className="btn-margin"
        onClick={this.login.bind(this)}
      >
        Log In
      </Button>
    </div>
  )}
  {isAuthenticated() && <MyLeagues />}
</div>; */
}
